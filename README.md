# Screen Top Theme

Layout theme for Universal Till POS: the sale screen (basket) spans the **top**
of the main page and the product buttons sit **underneath**, with journal and
tender beside them. Asset-only plugin (`runtime: "none"`); registers one
`type: "theme"` entry. After install, pick it under Settings → Theme.
