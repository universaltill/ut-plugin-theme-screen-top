name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate manifest + theme assets
        run: scripts/validate.sh
      - name: Package (dry run)
        run: scripts/package.sh
